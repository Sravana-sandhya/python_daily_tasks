# Q3 : Write a Python program using the math module to calculate and display the square root, floor value, and ceiling value of a number entered by the user. 
import math
num = float(input("Enter a number :"))
print(math.sqrt(num))
print(math.floor(num))
print(math.ceil(num))